package com.h2hyun37.biz.algorithm.ds.linkedList;

public class LinkedListClient {

	// public void someMethod() {
	//
	// Element head = null;
	// Element temp = null;
	//
	// int[] numbers = new int[] { 45, 3, 7, 30, 11, 19, 123 };
	//
	// for (int number : numbers) {
	// temp = new Element(number);
	//
	// if (head == null) {
	// head = temp;
	// } else {
	// Element temp2 = head;
	//
	// while (temp2.getNext() != null) {
	// temp2 = temp2.getNext();
	// }
	//
	// temp2.setNext(temp);
	// }
	// }
	//
	// printAll(head);
	//
	// // 중간에 add
	// temp = head;
	// for (int i = 0; i < 2; i++) {
	// temp = temp.getNext();
	// }
	//
	// Element temp2 = new Element(9999);
	// temp2.setNext(temp.getNext());
	// temp.setNext(temp2);
	//
	// printAll(head);
	//
	// }

}
